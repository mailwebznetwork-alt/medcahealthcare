# Global Content Variables (Patch 8)

## Location

**Settings → Global Content** (`/settings/global-content`)

## Tokens

`{{ company_name }}`, `{{ tagline }}`, `{{ phone_number }}`, `{{ whatsapp }}`, `{{ email }}`, `{{ address }}`, `{{ primary_cta }}`, `{{ secondary_cta }}`, `{{ business_hours }}`, `{{ website_url }}`

Interpolation: `GlobalContentInterpolator` in `ContentParser`.

## Features

- Edit all keys in one form
- **Live preview** sample headline / contact / CTA
- **Export / Import** JSON
- **Versioning** via `global_content_variable_snapshots` (Save & version, Restore)

## Permissions

Admin and super_admin only.
